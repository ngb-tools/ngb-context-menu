export interface ContextMenuPosition {
  x: string;
  y: string;
}
export interface NgbContextMenuItem {
  id: number;
  label?: string;
  icon?: string;
  iconTitle?: string;
  disabled?: boolean;
  isDivider?: boolean;
}
